rootProject.name = 'concurrency-control'

include 'module-core'
include 'module-api'

//modules 폴더에 집합
for (project in rootProject.children) {
    project.projectDir = file("modules/${project.name}")
}

